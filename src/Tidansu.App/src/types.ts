export enum LayoutType {
    /** In-app chrome: centered column + top nav (dashboard, pricing, account, space). */
    APP = 'app',
    /** Bare centered page, no nav (landing, auth). */
    PLAIN = 'plain',
}
