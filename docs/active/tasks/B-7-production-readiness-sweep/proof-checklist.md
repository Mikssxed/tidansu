# B-7 · Production-readiness sweep — Proof checklist

One row per FR-1…FR-12 acceptance criterion (FR-13 *is* this document). Status
legend: `✅ Verified <date>` · `⏳ Pending: <what unblocks it>` · `📋 Open deploy step`.

All runtime verification below was driven against the **built artifact**
(`npm run build` output served from `wwwroot`) with `ASPNETCORE_ENVIRONMENT=Production`
and a local SQL Server LocalDB instance (`(localdb)\MSSQLLocalDB`) standing in for
the production database — not the dev server, and not a live production deploy.

| # | Requirement | What was proven | How it was proven | Status |
|---|---|---|---|---|
| FR-1 | Full core journey (auth → spaces/zones/items → caps/paywall → billing) on a prod-like build | The **auth-request** leg (magic-link request accepted, no dev link/token/file leak, unauthenticated redirect to `/login` in a real browser) is proven prod-like (see FR-7/FR-8/FR-9). The **authenticated** legs (create space/zone/item, hit each cap with correct `reason`, Stripe upgrade) were **not** driven end-to-end in this pass: completing sign-in requires a real delivered email — the raw magic-link token is only ever held in memory and returned via `devLink`, which is `null` outside Development by design (confirmed in code and empirically, see FR-9); there is no dev-token backdoor to exploit for a "prod-like but no real inbox" walkthrough, and simulating an authenticated browser session by hand-injecting tokens into `localStorage` was considered but not attempted — it would prove the client reads the storage contract, not a real sign-in. The plan-cap/paywall `reason` logic itself (`PlanPolicy` in `Tidansu.Domain`) is pre-existing code untouched by this sweep and outside this task's scope to re-verify. | Static + runtime: read `RequestMagicLinkCommandHandler.cs` (token is hashed before persistence — `TokenHash = jwtService.HashRefreshToken(rawToken)`, raw value never stored); ran the prod-like boot and drove the request endpoint + the unauthenticated-redirect check (see FR-7/FR-8/FR-9 rows). | ⏳ Pending: real Brevo delivery (FR-2) unblocks a real end-to-end sign-in and the rest of the authenticated journey; owner completes B-4 Brevo setup (or supplies any working SMTP relay), then this leg is fully drivable using the delivered email link. |
| FR-2 | Real email delivery via a live mail provider | Not attempted — no live Brevo account configured in this environment. | N/A | ⏳ Pending owner Brevo setup (`SmtpSettings__Host/Username/Password/Port/EnableSsl` pointed at the real Brevo SMTP relay per `docs/active/tasks/B-4-real-login-email/SETUP.md`) |
| FR-3 | Live Stripe test-mode purchase → Pro; cancellation → Free | Not attempted — no Stripe test-mode account/webhook endpoint configured in this environment. | N/A | ⏳ Pending owner Stripe test-mode drive (per `docs/active/tasks/B-6-connect-real-stripe/go-live-cutover.md`) |
| FR-4 | No production secret committed to the repository | No Stripe secret/webhook key pattern (`sk_live`, `sk_test`, `whsec_`) or a populated `"Password": "..."` value found anywhere in the current tree or the full git history. The only non-blank secret-shaped value in any `appsettings*.json` is `JwtSettings:Secret` in `appsettings.Development.json`, which is clearly named as a local-dev-only value in a file never used in Production (guarded by BA-1's sibling — the existing `JwtSettings:Secret` production guard rejects short/blank secrets, so this dev value could never satisfy a real deploy anyway). | `git grep -inE "sk_live\|sk_test\|whsec_" -- 'src/**/appsettings*.json'` → no hits; `git grep -inE '"password"\s*:\s*"[^"]+' -- 'src/**/appsettings*.json'` → no hits; `git log --all -p -- 'src/**/appsettings*.json' \| grep -inE "sk_live\|sk_test\|whsec_"` → no hits; `git log --all -p \| grep -inE "sk_live_[A-Za-z0-9]\|sk_test_[A-Za-z0-9]\|whsec_[A-Za-z0-9]"` (whole-repo history) → no hits. Read `appsettings.Development.json` directly: `JwtSettings:Secret` is `"DevSecretKeyForLocalDevelopmentOnlyAtLeast32Chars!"`. | ✅ Verified 2026-07-13 |
| FR-5 | Missing/invalid required production config fails loud, names the key, never echoes the value | All four guards fire correctly and independently in a live `ASPNETCORE_ENVIRONMENT=Production` boot: (a) blank `AppSettings:FrontendUrl` → `InvalidOperationException: AppSettings:FrontendUrl is missing. Set the AppSettings__FrontendUrl environment variable.` (BA-1, new); (b) blank `ConnectionStrings:TidansuDb` → `InvalidOperationException: ConnectionStrings:TidansuDb is missing. Set the ConnectionStrings__TidansuDb environment variable.` (BA-2, new); (c) blank `JwtSettings:Secret` → existing guard fires unchanged; (d) `StripeSettings:Enabled=true` with blank `SecretKey` → existing guard fires unchanged, naming all three missing Stripe keys. No case echoed any configured value — only key names. | Ran `dotnet bin/Debug/net10.0/Tidansu.API.dll` four times under `ASPNETCORE_ENVIRONMENT=Production`, each time isolating exactly one blank setting while the rest were valid (SMTP host/user/pass/port/ssl, JWT secret, connection string, frontend URL as applicable); captured the unhandled-exception output for each. | ✅ Verified 2026-07-13 |
| FR-6 | Cross-origin requests rejected unless from the configured origin; blank config fails closed | With `AppSettings__FrontendUrl` set to `https://app.tidansu.example`, a CORS preflight (`OPTIONS` + `Access-Control-Request-Method`) from `Origin: https://evil.example` returns `204` with **no** `Access-Control-Allow-Origin` header (browser will block it) — confirms fail-closed for an unlisted origin. A GET from the same evil origin similarly carries no CORS header. **Rough edge found:** a GET/preflight from the *configured* origin (`https://app.tidansu.example`) also received no `Access-Control-Allow-Origin` header in this local test — logged to the FR-12 table below for owner review; it does not block this sweep because, per Owner decision OQ-2 (single-origin topology), the shipped SPA and API share one origin, so the browser will treat those calls as same-origin and never invoke CORS at all — this only matters for a hypothetical future split deployment. Blank `AppSettings:FrontendUrl` in Production no longer silently boots into a zero-origin CORS policy — it now fails loud at startup instead (see FR-5), which is a *stronger* fail-closed guarantee than "blank → no origins allowed". | Booted the prod-like build with `AppSettings__FrontendUrl=https://app.tidansu.example`; `curl -i -X OPTIONS -H "Origin: https://evil.example" -H "Access-Control-Request-Method: GET" http://localhost:5099/api/spaces`; repeated with `Origin: https://app.tidansu.example`; repeated as plain GET against the anonymous `/api/plans` endpoint for both origins. | ✅ Verified 2026-07-13 (fails closed for unlisted origins; legit-origin CORS-header gap logged to FR-12, non-blocking under the approved single-origin topology) |
| FR-7 | Auth bypass compiled out of the production bundle; cannot be re-enabled by any runtime env var | Grepped the actual `vite build` output (`src/Tidansu.API/wwwroot/assets/*.js`) for `VITE_DISABLE_AUTH` and `authDisabled` — **no trace** in any emitted JS asset, confirming the `import.meta.env.DEV` gate dead-code-eliminates the bypass branch entirely at build time (a runtime environment variable cannot resurrect code that was never emitted — there is no reference to that name anywhere in the shipped JS for a runtime value to even reach). Also drove the **actual built SPA in a real (headless) browser** against the Production API: navigating to the protected `/spaces` route while unauthenticated redirected to `/login?returnUrl=/spaces` and rendered the real sign-in page — confirming `requiresAuth` enforcement holds in the shipped build, not just in source. | `npm run build` then `grep -r "VITE_DISABLE_AUTH\|authDisabled" src/Tidansu.API/wwwroot/assets/` → no matches. Separately: booted the prod-like build (serving `wwwroot` same-origin), launched headless Edge (`msedge --headless=new --remote-debugging-port=9222`), drove it via a hand-rolled Chrome DevTools Protocol client over Node's WebSocket, navigated to `http://localhost:5099/spaces`, and read `window.location.href` after the SPA settled → `http://localhost:5099/login?returnUrl=/spaces`, with the rendered page body confirming the sign-in view ("Sign in or create your account"). | ✅ Verified 2026-07-13 |
| FR-8 | Dev email-to-file path inert outside Development; real send attempted; delivery failure surfaces as a clear error, not a silent success | Ran the prod-like build with an unreachable SMTP target (`localhost:1`, connection refused instantly) and requested a magic link: got `HTTP 500` with body `{"errors":{"general":["Something went wrong."]},"isSuccess":false}` — a clear, sanitized error, not a silent 200. No file was written under `DevelopmentEmails/` during the run (directory's newest file predates this test by 5 days — nothing new created). Logs show only `Email` + a generic `Reason` ("Failure sending mail.") — no SMTP credentials, email body, or magic link value logged. | Booted with `SmtpSettings__Host=localhost`, `Port=1`; `curl -i -X POST -d '{"email":"prodtest@example.com"}' http://localhost:5099/api/auth/magic-link`; inspected `Logs/Tidansu-Api-*.txt` and `DevelopmentEmails/` directory listing before/after. | ✅ Verified 2026-07-13 |
| FR-9 | No dev magic-link/token in any production API response | A **successful** (`200 OK`) magic-link request against the prod-like build (using a local fake-SMTP stub that accepts the send so the happy path completes) returned body `{"data":{"devLink":null},"errors":{},"isSuccess":true}` — `devLink` is `null`, confirming `MagicLinkEmailSender.SendAsync` returns `null` outside Development exactly as coded. | Ran a minimal local SMTP stub on `127.0.0.1:2525` (accepts the SMTP dialogue, no real delivery) so the send path succeeds; booted the prod-like build pointed at it; `curl -i -X POST -d '{"email":"prodsuccess@example.com"}' http://localhost:5099/api/auth/magic-link` → inspected the response body directly. | ✅ Verified 2026-07-13 |
| FR-10 | Rate limiter partitions on the real client IP behind the actual production reverse proxy | Config is now env-driven (`ForwardedHeaders:KnownProxies` / `ForwardedHeaders:KnownNetworks`, bound in `Program.cs`): a literal `"*"` in either list is rejected at startup (`InvalidOperationException`); a valid IP/CIDR (e.g. `203.0.113.5`, `10.0.0.0/8`) parses correctly and the app boots and serves requests normally; blank/absent config leaves the framework loopback-only default untouched (verified by the earlier fail-loud tests all booting/crashing for unrelated reasons with no `ForwardedHeaders__*` set at all — no crash from this code path). The actual production proxy address and the "two client IPs rate-limited independently" check cannot be completed until the real topology is known. | Booted with `ForwardedHeaders__KnownProxies__0=*` → `InvalidOperationException: ForwardedHeaders:KnownProxies must not contain a wildcard ("*")...`; booted with `ForwardedHeaders__KnownProxies__0=203.0.113.5` and `ForwardedHeaders__KnownNetworks__0=10.0.0.0/8` → app started and `curl http://localhost:5099/` returned `200`. | 📋 Open deploy step: set `ForwardedHeaders__KnownProxies`/`ForwardedHeaders__KnownNetworks` to the real production proxy address(es) once topology is known (Owner decision OQ-3); then verify two distinct real client IPs behind that proxy are rate-limited independently, not sharing one bucket. Wildcard-rejection and valid-config paths are ✅ verified 2026-07-13. |
| FR-11 | No dev-tools overlay in the production bundle; no per-query DB chatter in production logs | (a) Grepped the built `wwwroot/assets/*.js` for `vue-devtools`/`__VUE_DEVTOOLS` — **no matches**; the plugin already self-excludes from a `vite build` (current `vite-plugin-vue-devtools` version applies `apply: 'serve'` internally), so **no code edit was needed** (FE-1 was verify-only). (b) `Microsoft.EntityFrameworkCore` Serilog override in the base `appsettings.json` changed from `Information` → `Warning` (BA-4); during the FR-8/FR-9 runtime tests (which included real DB reads/writes — `InvalidateActiveForEmailAsync`, `AddAsync` on `MagicLinkTokens`), the log file contained **zero** `Executed DbCommand`/EF Core lines, while the application-level `Information` logs (magic-link issuance, email send) and `Error` logs (SMTP failure) remained fully visible. | `npm run build` then `grep -r "vue-devtools\|__VUE_DEVTOOLS" src/Tidansu.API/wwwroot/assets/` → no matches. Edited `appsettings.json` line 31; ran the FR-8/FR-9 prod-like boots and `grep -i "Executed DbCommand\|Microsoft.EntityFrameworkCore" Logs/Tidansu-Api-*.txt` → 0 matches, while the handler's own `Information`/`Error` log lines were present. | ✅ Verified 2026-07-13 |
| FR-12 | Every rough edge found is fixed on the spot or logged, none left unaccounted for | See the rough-edge table below — everything found during this sweep is listed there with a disposition. Nothing was left unaccounted for. | Tracked during FR-6/V-1/V-3 runtime testing. | ✅ Verified 2026-07-13 — table below, batched for owner review per Owner decision OQ-4 (not auto-filed) |

## FR-12 rough-edge log (batched for owner review — not auto-filed, per Owner decision OQ-4)

| # | Finding | Where observed | Severity / disposition |
|---|---|---|---|
| 1 | A CORS preflight/GET from the **exact configured** `AppSettings:FrontendUrl` origin does not receive an `Access-Control-Allow-Origin` header in a local test (same as an unlisted origin gets — i.e. CORS currently fails closed for *everyone*, not just untrusted origins). | Local prod-like boot, `curl` against `/api/plans` and `/api/spaces` with `Origin: https://app.tidansu.example` (the exact value of `AppSettings__FrontendUrl`). | Low severity / not fixed here (out of the four approved code edits, and CORS is explicitly "defence-in-depth only" per Owner decision OQ-2 — the shipped topology is single-origin, so the browser never invokes CORS for same-origin calls in the first place). **Batched for owner review**; if a future split deployment needs CORS to actually grant access to a legitimate second origin, this needs a follow-up investigation (candidate cause: CORS policy evaluation happening before endpoint/route metadata is available in the minimal-hosting pipeline — untested hypothesis, not confirmed). Does not read as a security regression (it fails safe, arguably too safe) or a scalability concern, so it is **not** routed to B-8. |

## Deploy-time configuration

See [`./deploy-config.md`](./deploy-config.md) for the full enumeration of every
environment-driven setting a production deploy must supply, and the consequence
of leaving each blank.

## Review-fix hardening (2026-07-13)

Three findings from `review.md` (N1/N2) and `security-review.md` (S-M2/S-L1/S-M1)
were fixed after the initial sweep landed:

| # | Finding | Fix | How verified |
|---|---|---|---|
| F1 | N1/S-M2 — `::/0` (and the unspecified `::`) IPv6 all-networks wildcard was not rejected, only `"*"`/`0.0.0.0/0` were. | `Program.cs` `KnownNetworks` guard now rejects `"*"`, `"0.0.0.0/0"`, `"::/0"`, and `"::"`, trimmed and compared case-insensitively (same treatment now applied to the `KnownProxies` `"*"` check). | Booted Development with `ForwardedHeaders__KnownNetworks__0="::/0"` → `InvalidOperationException: ForwardedHeaders:KnownNetworks must not contain a wildcard ("*", "0.0.0.0/0", "::/0", or "::"). List explicit CIDR ranges.` A valid CIDR (`10.0.0.0/24`) plus a valid proxy IP (`10.0.0.5`) still boot normally — regression-checked. |
| F2 | N2/S-L1 — a malformed proxy IP or CIDR reached `IPAddress.Parse`/`IPNetwork.Parse` and threw a bare `FormatException` naming neither the config key nor the bad value. | Switched to `IPAddress.TryParse`/`IPNetwork.TryParse`; on failure throw a keyed `InvalidOperationException` naming the config key and the offending value (not a secret, safe to echo per the review). | Booted Development with `ForwardedHeaders__KnownProxies__0="not-an-ip"` → `InvalidOperationException: ForwardedHeaders:KnownProxies contains an invalid IP address: 'not-an-ip'.`; with `ForwardedHeaders__KnownNetworks__0="10.0.0.0/33"` → `InvalidOperationException: ForwardedHeaders:KnownNetworks contains an invalid CIDR range: '10.0.0.0/33'.` |
| F3 | S-M1 — the BA-1 (`AppSettings:FrontendUrl`) and BA-2 (`ConnectionStrings:TidansuDb`) guards fired only on `IsProduction()`, so a mis-named non-dev environment (e.g. `Staging`) would boot past them with blank config. | Both guards now key off `!IsDevelopment()` (matching the existing SMTP guard's scoping), so any non-Development environment fails loud identically. **Accepted tradeoff (owner):** Staging now also requires the full prod config set (FrontendUrl, connection string) to be present — it can no longer boot half-configured. | Booted `ASPNETCORE_ENVIRONMENT=Staging` with blank `ConnectionStrings__TidansuDb` (and valid everything else) → `InvalidOperationException: ConnectionStrings:TidansuDb is missing...`; with a valid connection string but blank `AppSettings__FrontendUrl` (and valid SMTP/Stripe) → `InvalidOperationException: AppSettings:FrontendUrl is missing...`. Regression-checked: `ASPNETCORE_ENVIRONMENT` **unset** with blank connection string still fails loud (defaults to Production, unaffected by this change); `ASPNETCORE_ENVIRONMENT=Development` still boots with the real dev connection string/FrontendUrl untouched. |

`dotnet build` (whole solution) after all three fixes: **0 errors**, 12 pre-existing
`NU1903` transitive-package-CVE warnings (AutoMapper/System.Security.Cryptography.Xml/
Microsoft.OpenApi — not introduced by this change, tracked under B-8).

Not re-driven: the S-M2 spoof-test (forging `X-Forwarded-For` over a live IPv6
connection with `::/0` trusted) and the FR-10 real-proxy end-to-end drive — both
require network/topology setup beyond this fix's scope; the startup-rejection
behaviour that prevents the spoof from being possible is directly verified above.
